apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
{%- if values.byoAgent %}
patchesStrategicMerge:
  - deployment-patch.yaml
{%- endif %}
resources:
  - ../../base
  {%- if values.byoAgent %}
  - tekton-repositories/
  {%- endif %}
